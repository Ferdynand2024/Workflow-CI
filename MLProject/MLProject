name: CBR_Disease_Prediction_MochamadFerdynandWinarto

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      n_neighbors:
        type: int
        default: 3
      metric:
        type: str
        default: euclidean
      weights:
        type: str
        default: uniform
    command: >
      python modelling.py
